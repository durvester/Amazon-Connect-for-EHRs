# infra/lib/

One file per CDK stack. Stacks are composed in `bin/app.ts`. Real stacks land in Session 0007 — this directory is currently empty.

Planned files:
- `data-stack.ts`
- `api-stack.ts`
- `agent-stack.ts`
- `connect-stack.ts`
