# tools/complete_verification

Lambda tool the agent calls to record verification outcome on the Connect contact. Calls `UpdateContactAttributes` to set `verified` / `patient_id`.

**Status:** scaffold. Implemented in Session 0004.
