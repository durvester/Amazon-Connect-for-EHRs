# Fixtures

Recorded FHIR Bundle responses used by `test_fhir_client.py` and `test_handler.py`.

Populated in Session 0003 from real Practice Fusion responses (with patient data anonymized). Until then this directory is intentionally empty.
